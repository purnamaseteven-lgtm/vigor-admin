# Smoke Run Report

Date: 2026-05-05
Scope: `docs/FEATURE_SMOKE_CHECKLIST.md`

## Automated Results
- Pass: `npm run test:wiring`
- Pass: `npm run build`
- Pass: `node --check server/index.js`
- Blocked: `node tests/seamless-wallet.test.js` (missing `VITE_SUPABASE_URL`)

## Checklist Status

### 1) Auth & Access
- Blocked: Requires running app with valid Supabase session in browser.

### 2) Members/Company/Admin
- Partial: Wiring confirmed by static regression test.
- Blocked: CRUD runtime verification requires live app + DB.

### 3) Finance & Wallet
- Partial: Wiring confirmed (`dbApproveDeposit`, `dbRejectWithdrawal`, `dbAdjustMemberBalance` paths exist).
- Blocked: Transaction behavior needs live DB test.

### 4) Payment Providers
- Partial: API and webhook endpoints present and mounted.
- Blocked: Requires real provider credentials and callback execution.

### 5) Seamless Wallet
- Partial: Endpoints exist and syntax is valid.
- Blocked: Integration test cannot run without Supabase URL and live dataset.

### 6) CRM
- Partial: CRUD wiring present across segments/missions/tournaments/automation/push.
- Blocked: End-to-end behavior needs runtime verification.

### 7) Customization/Tools
- Partial: Save paths exist; Cloudflare path can return mock when token missing.
- Blocked: Needs configured external services for non-mock validation.

### 8) Final Verification
- Pass: Frontend build
- Pass: Backend syntax check
- Pass: Wiring regression test

## Blocking Inputs Needed To Complete Full Smoke
1. `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`
2. Backend `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`
3. Provider credentials: `UNOPAY_*`, `COIN2PAY_*`, `SAWALA_*`
4. Seamless keys: `PG_OPERATOR_TOKEN`, `PG_SECRET_KEY`, whitelist config
5. Cloudflare token config (for host tooling non-mock verification)
